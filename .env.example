# Copy to .env.local and adjust as needed. All values are optional —
# the app ships with working defaults and runs fully offline with mocks.

# Stellar network to target: testnet | mainnet
VITE_NETWORK=testnet

# Soroban RPC endpoint
VITE_RPC_URL=https://soroban-testnet.stellar.org

# Deployed vault contract id
VITE_VAULT_CONTRACT=CDLZFC3SYJYDZT7K67VZ75HPJVIEUVNIXF47ZG2FB2RMQQVU2HHGCYSC
